/// <reference types="react-scripts" />

interface IJSWindow {
	google: any;
}

interface INavbarButton {
	buttonText: string;
	path:string;
}

interface IBlogButton{
	buttonText: string;
	buttonValue: string;
}